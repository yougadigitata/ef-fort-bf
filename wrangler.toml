name = "ef-fort-bf"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# ⚠️ SÉCURITÉ : Les secrets sensibles (SUPABASE_KEY, JWT_SECRET) doivent être
# définis via `wrangler secret put` et NON commitées ici.
# Commandes : wrangler secret put SUPABASE_KEY
#             wrangler secret put JWT_SECRET

[vars]
SUPABASE_URL = "https://xqifdbgqxyrlhrkwlyir.supabase.co"
# SUPABASE_KEY et JWT_SECRET → définis comme secrets Cloudflare Workers
# wrangler secret put SUPABASE_KEY
# wrangler secret put JWT_SECRET
